[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip", "ffmpeg", "curl"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install --upgrade yt-dlp"
]

[start]
cmd = "node server.js"
